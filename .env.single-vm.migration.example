# Supply this file only to the one-shot `migrate` service, then remove it from the VM release.
# The password must be URL encoded.
DATABASE_MIGRATION_URL=postgresql://openround_owner:replace-owner-password-with-url-safe-random-value@postgres:5432/openround
